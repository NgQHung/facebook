import React from 'react';
import DefaultLayout from '../../layouts/DefaultLayout';

const Home = () => {
    return <DefaultLayout className="bg-secondary">index</DefaultLayout>;
};

export default Home;
